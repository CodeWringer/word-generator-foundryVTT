{{!-- Params:
listItem: WordGeneratorSettings
sequencingStrategies: Array<DropDownOption>
spellingStrategies: Array<DropDownOption>
--}}
<li class="word-generator-setting-entry word-generator-card">
  {{!-- Move controls --}}
  {{> "modules/word-generator/template/move-controls.hbs" listItemId=listItem.id }}
  <div class="word-generator-flex flexcol grow">
    {{!-- Header Row --}}
    <div class="word-generator-flex column">
      <div class="word-generator-flex middle">
        {{!-- Delete Button --}}
        <button 
          class="word-generator-square-button danger"
          type="button" 
          id="{{listItem.id}}-delete"
          data-id="{{listItem.id}}"
          title="{{localize "wg.generator.delete"}}"
          >
          <i class="fas fa-trash"></i>
        </button>
        {{!-- Name Input --}}
        <input 
          class="word-generator-input-textfield"
          type="text"
          id="{{listItem.id}}-name"
          data-id="{{listItem.id}}"
          placeholder="{{localize "wg.generator.placeholder.name"}}"
          value="{{listItem.name}}"
        />
      </div>
      <div class="word-generator-flex grow"></div>
    </div>
    {{#if listItem.collapsed}}
    {{else}}
    {{!-- Sample Set --}}
    <div class="flexrow">
      <label for="{{listItem.id}}-sampleSet">{{localize "wg.generator.sampleSet.field"}}</label>
      <i class="far fa-question-circle word-generator-info sample-set" id="{{listItem.id}}"></i>
      <span>{{localize "wg.generator.sampleSize"}}: {{listItem.sampleSet.length}}</span>
      <button 
        class="word-generator-square-button"
        type="button" 
        id="{{listItem.id}}-edit-sample-set"
        data-id="{{listItem.id}}"
        title="{{localize "wg.generator.sampleSet.edit"}}"
        >
        <i class="fas fa-edit"></i>
      </button>
    </div>
    {{!-- targetLengthMin --}}
    <div class="flexrow">
      <label for="{{listItem.id}}-targetLengthMin">{{localize "wg.generator.targetLength.min"}}</label>
      <i class="far fa-question-circle word-generator-info target-length" id="{{listItem.id}}"></i>
      <input id="{{listItem.id}}-targetLengthMin" type="number" min="1" value="{{listItem.targetLengthMin}}" />
    </div>
    {{!-- targetLengthMax --}}
    <div class="flexrow">
      <label for="{{listItem.id}}-targetLengthMax">{{localize "wg.generator.targetLength.max"}}</label>
      <i class="far fa-question-circle word-generator-info target-length" id="{{listItem.id}}"></i>
      <input id="{{listItem.id}}-targetLengthMax" type="number" min="1" value="{{listItem.targetLengthMax}}" />
    </div>
    {{!-- entropy --}}
    <div class="flexrow">
      <label for="{{listItem.id}}-entropy">{{localize "wg.generator.entropy.label"}}</label>
      <i class="far fa-question-circle word-generator-info entropy" id="{{listItem.id}}"></i>
      <input id="{{listItem.id}}-entropy" type="number" min="0" max="1" value="{{listItem.entropy}}" />
    </div>
    {{!-- entropyStart --}}
    <div class="flexrow">
      <label for="{{listItem.id}}-entropyStart">{{localize "wg.generator.entropy.start.label"}}</label>
      <i class="far fa-question-circle word-generator-info entropy-start" id="{{listItem.id}}"></i>
      <input id="{{listItem.id}}-entropyStart" type="number" min="0" max="1" value="{{listItem.entropyStart}}" />
    </div>
    {{!-- entropyMiddle --}}
    <div class="flexrow">
      <label for="{{listItem.id}}-entropyMiddle">{{localize "wg.generator.entropy.middle.label"}}</label>
      <i class="far fa-question-circle word-generator-info entropy-middle" id="{{listItem.id}}"></i>
      <input id="{{listItem.id}}-entropyMiddle" type="number" min="0" max="1" value="{{listItem.entropyMiddle}}" />
    </div>
    {{!-- entropyEnd --}}
    <div class="flexrow">
      <label for="{{listItem.id}}-entropyEnd">{{localize "wg.generator.entropy.end.label"}}</label>
      <i class="far fa-question-circle word-generator-info entropy-end" id="{{listItem.id}}"></i>
      <input id="{{listItem.id}}-entropyEnd" type="number" min="0" max="1" value="{{listItem.entropyEnd}}" />
    </div>
    {{!-- endingPickMode --}}
    <div class="flexrow">
      <label for="{{listItem.id}}-endingPickMode">{{localize "wg.generator.endingPickMode.label"}}</label>
      <i class="far fa-question-circle word-generator-info ending-pick-mode" id="{{listItem.id}}"></i>
      <select id="{{listItem.id}}-endingPickMode">
        <option value="0">{{localize "wg.generator.endingPickModes.none"}}</option>
        <option value="1">{{localize "wg.generator.endingPickModes.random"}}</option>
        <option value="2">{{localize "wg.generator.endingPickModes.followBranch"}}</option>
      </select>
    </div>
    {{!-- sequencingStrategy --}}
    <div class="word-generator-card">
      <div class="flexrow">
        <label for="{{listItem.id}}-sequencingStrategy">{{localize "wg.generator.sequencingStrategy.label"}}</label>
        <i class="far fa-question-circle word-generator-info sequencing-strategy" id="{{listItem.id}}"></i>
        <select id="{{listItem.id}}-sequencingStrategy">
          {{#each sequencingStrategies as |strategy index|}}
          <option value="{{strategy.value}}">{{strategy.localizedTitle}}</option>
          {{/each}}
        </select>
      </div>
      {{!-- Sequencing Settings --}}
      <ul id="{{listItem.id}}-sequencing-settings" class="word-generator-unstyled-list">
        {{#each listItem.sequencingStrategySettings as |setting index|}}
          {{> "modules/word-generator/template/setting-list-item.hbs" item=setting}}
        {{/each}}
      </ul>
    </div> 
    {{!-- spellingStrategy --}}
    <div class="word-generator-card">
      <div class="flexrow">
        <label for="{{listItem.id}}-spellingStrategy">{{localize "wg.generator.spellingStrategy.label"}}</label>
        <i class="far fa-question-circle word-generator-info spelling-strategy" id="{{listItem.id}}"></i>
        <select id="{{listItem.id}}-spellingStrategy">
          {{#each spellingStrategies as |strategy index|}}
          <option value="{{strategy.value}}">{{strategy.localizedTitle}}</option>
          {{/each}}
        </select>
      </div>
      {{!-- Spelling Settings --}}
      <ul id="{{listItem.id}}-spelling-settings" class="word-generator-unstyled-list">
      {{#each listItem.spellingStrategySettings as |setting index|}}
        {{> "modules/word-generator/template/setting-list-item.hbs" item=setting}}
      {{/each}}
      </ul>
    </div>
    {{!-- Seed --}}
    <div class="word-generator-flex row">
      <label for="{{listItem.id}}-seed">{{localize "wg.generator.seed.label"}}</label>
      <i class="far fa-question-circle word-generator-info seed" id="{{listItem.id}}"></i>
      <input id="{{listItem.id}}-seed" type="text" value="{{listItem.seed}}" />
    </div>
    {{/if}}
    {{!-- Collapse Button --}}
    <div class="word-generator-flex" style="max-height: 2rem">
      <a
        class="word-generator-flex grow center button"
        id="{{listItem.id}}-collapse"
        >
        <span class="word-generator-flex center word-generator-square">
          {{#if listItem.collapsed}}
          <i class="fas fa-angle-double-down"></i>
          {{else}}
          <i class="fas fa-angle-double-up"></i>
          {{/if}}
        </span>
      </a>
    </div>
  </div>
  {{!-- Generate --}}
  <div class="word-generator-flex">
    <button 
      class="word-generator-square-button grow"
      type="button" 
      id="{{listItem.id}}-generate"
      data-id="{{listItem.id}}"
      title="{{localize "wg.generator.generate"}}"
      >
      <i class="fas fa-pen-nib"></i>
    </button>
  </div>
</li>